# Control and Reaction Plan

The machine-readable table is `outputs/v3/tables/control_reaction_plan.csv`. It maps each selected failure type to monitoring signal, threshold logic, owner, containment, diagnostic step, escalation and verification. Analytics therefore terminate in an operational decision pathway rather than in a dashboard-only finding.
