# Data Contracts

Machine-readable schemas live in `schemas/`. Production events, telemetry, maintenance, supplier lots, CAPA and quality-event views have explicit keys, types, allowed categories and temporal constraints. Validation checks duplicate IDs, missing core identifiers, impossible durations, timestamp bounds, station/machine counts and failure-catalog consistency.
